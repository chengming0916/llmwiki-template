# CLAUDE.md

> Claude Code 入口文件。完整规则见 [[AGENTS]]。

---

## 核心指令

1. 首先完整阅读并严格遵守 [[AGENTS]] 文件中的所有规则
2. 禁止幻觉，严格基于本地笔记内容回答
3. 不确定时明确说明"未找到相关内容"，不猜测

## 安全原则

- **只读 raw/**：`raw/` 目录为人类原始素材，只读不修改
- **谨慎写操作**：修改笔记前先读取确认，保留 YAML frontmatter
- **保护数据**：不删除笔记，不覆盖未备份的修改

## Obsidian 规范

- 内部链接：`[[页面名称]]`
- YAML frontmatter 必须在文件顶部
- 嵌入图片：`![[image.png]]`
- 代码块：````python`, ````bash` 等
